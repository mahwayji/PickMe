export type User = {
    id: string,
    username: string,
    email: string,
    firstName: string,
    lastName: string,
    profilePicture: string,
    isAdmin: Boolean,
    createdAt: string,
    updatedAt: string,
}